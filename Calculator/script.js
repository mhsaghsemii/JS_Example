// let Definition for Calculator

let calc = 0,num1= 16, num2 = 4

// Sum Operation of two Variable
calc = num1 + num2
// Show Sum
console.log(`${num1} + ${num2}` = calc);

// ------------------------------

// Minus Operation of two Variable
calc = num1 - num2
// Show Minus
console.log(`${num1} - ${num2}` = calc);

// ------------------------------

// Multiply Operation of two Variable
calc = num1 - num2
// Show Multiply
console.log(`${num1} * ${num2}` = calc);

// ------------------------------

// Division Operation of two Variable
calc = num1 - num2
// Show Division
console.log(`${num1} / ${num2}` = calc);