// Variable Definition for Compare
let string = 'Hello World!', target = 'World'

// Show the string Variable
console.log('String: ',string)
console.log('Target: ',target)

// Compare two Variable 'string' and 'target'
if(string.includes(target)){
    // the Output if Condition is True
    console.log(`the ${string} contains the ${target}`)
}else{
        // the Output if Condition is False
    console.log(`the ${string} doesn't contains the ${target}`)
}